import { makeStyles, createStyles, Theme } from "@material-ui/core/styles";
export const listedUsersPageStyles = makeStyles((theme: Theme) =>
  createStyles({
    root: {
      flexGrow: 1,
    },
  })
);
